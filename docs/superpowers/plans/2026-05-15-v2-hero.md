# /v2 Alternate Hero Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a `/v2` route rendering `Nav` plus an alternate hero with new copy and a retuned layout, leaving `/` untouched.

**Architecture:** A single self-contained server component file `src/app/v2/page.tsx`. The alternate hero and its backdrop are copied from the current hero (not shared) so iterating on `/v2` never affects `/`. `Nav` and `AccessButton` are imported from existing modules; `AccessModalProvider` is already supplied globally by `src/app/layout.tsx`, so the access button works on `/v2` with no extra wiring.

**Tech Stack:** Next.js 16 App Router, React 19, Tailwind CSS v4. No test framework in this repo — verification is `npm run build` (typecheck + compile) and `npm run lint`, plus a dev-server visual check.

---

### Task 1: Create the /v2 route with the alternate hero

**Files:**
- Create: `src/app/v2/page.tsx`
- Reference (do not modify): `src/app/page.tsx:85-164` (current `HeroBackdrop` + `Hero`), `src/app/nav.tsx:26`, `src/app/access-modal.tsx:106`

- [ ] **Step 1: Create `src/app/v2/page.tsx`**

Create the file with exactly this content. `HeroBackdrop` is copied verbatim from `src/app/page.tsx` (brand language, unchanged). The hero header drops the `max-w-[16ch]` constraint so the 4-word line sets large; the subhead keeps a readable measure and the header→subhead gap is tightened to `mt-8`.

```tsx
import { Nav } from "../nav";
import { AccessButton } from "../access-modal";

/* Alternate site — testbed at /v2. Nav + an alternate hero only;
   nothing below it. Copied from the real hero (not shared) so
   iterating here never affects /. */

/* Hero backdrop — hairline grid with the brand-mark hexagon outlined
   in accent yellow, top-right. Copied verbatim from src/app/page.tsx;
   this is fixed brand language. */
function HeroBackdrop() {
  const hexVertices: [number, number][] = [
    [36, 36],
    [136, 36],
    [204, 104],
    [204, 204],
    [104, 204],
    [36, 136],
  ];
  return (
    <div
      aria-hidden
      className="pointer-events-none absolute inset-0 overflow-hidden"
    >
      <div
        className="absolute inset-0"
        style={{
          backgroundImage: `
            linear-gradient(to right, var(--rule) 1px, transparent 1px),
            linear-gradient(to bottom, var(--rule) 1px, transparent 1px)
          `,
          backgroundSize: "48px 48px",
          WebkitMaskImage:
            "radial-gradient(ellipse 100% 70% at 50% 35%, black 40%, transparent 100%)",
          maskImage:
            "radial-gradient(ellipse 100% 70% at 50% 35%, black 40%, transparent 100%)",
          opacity: 0.7,
        }}
      />
      <svg
        viewBox="0 0 240 240"
        fill="none"
        className="absolute -right-16 top-1/2 h-[420px] w-[420px] -translate-y-1/2 md:right-0 md:h-[520px] md:w-[520px] lg:h-[600px] lg:w-[600px]"
      >
        <path
          d="M 36 36 L 136 36 L 204 104 L 204 204 L 104 204 L 36 136 Z"
          stroke="var(--accent)"
          strokeWidth="1.2"
          strokeLinejoin="miter"
        />
        <g stroke="var(--accent)" strokeWidth="1">
          {hexVertices.map(([x, y]) => (
            <g key={`${x}-${y}`}>
              <line x1={x - 4} y1={y} x2={x + 4} y2={y} />
              <line x1={x} y1={y - 4} x2={x} y2={y + 4} />
            </g>
          ))}
        </g>
      </svg>
    </div>
  );
}

/* HERO (v2) — same bones as the real hero, retuned for a short,
   benefit-led header: headline runs wide (no max-w clamp), subhead
   sits closer (mt-8) and keeps a readable measure. */
function Hero() {
  return (
    <section className="relative w-full overflow-hidden bg-[var(--bg)] pt-24 pb-32 md:pt-36 md:pb-44 lg:pt-40 lg:pb-52">
      <HeroBackdrop />
      <div className="relative mx-auto w-full max-w-[78rem] px-6 md:px-10">
        <h1 className="rise rise-2 display-1">
          Revenue, without the guesswork.
        </h1>
        <p className="rise rise-3 lead mt-8 max-w-[48ch] text-[var(--ink-soft)]">
          Kithos pulls together the scattered context behind your market,
          accounts, conversations, and outcomes, into sharper account
          decisions, stronger outreach, better meetings, and a sales
          motion that improves with every outcome.
        </p>
        <div className="rise rise-4 mt-12">
          <AccessButton size="lg" />
        </div>
      </div>
    </section>
  );
}

export default function V2Page() {
  return (
    <>
      <Nav />
      <main id="main">
        <Hero />
      </main>
    </>
  );
}
```

- [ ] **Step 2: Verify the build passes (typecheck + compile)**

Run: `npm run build`
Expected: build completes with no errors; output lists the `/v2` route alongside `/`.

- [ ] **Step 3: Verify lint passes**

Run: `npm run lint`
Expected: no errors.

- [ ] **Step 4: Visual check in dev server**

Run: `npm run dev`, open `http://localhost:3000/v2`.
Expected: page shows `Nav` and the hero only — nothing below the hero. Header reads "Revenue, without the guesswork."; the exact subhead text is present; the access button opens the modal. The hex-grid backdrop sits top-right. Confirm `http://localhost:3000/` is visually unchanged.

- [ ] **Step 5: Commit**

```bash
git add src/app/v2/page.tsx docs/superpowers/plans/2026-05-15-v2-hero.md
git commit -m "feat: add /v2 alternate hero testbed"
```

---

## Self-Review

**Spec coverage:**
- New route `src/app/v2/page.tsx` rendering `Nav` + one hero, nothing else → Task 1 Step 1. ✓
- `/` and shared components untouched (hero copied, not extracted) → Task 1 Step 1 (no imports from `page.tsx`; HeroBackdrop copied) + Step 4 (visual confirm `/` unchanged). ✓
- Header "Revenue, without the guesswork." + exact verbatim subhead + unchanged `AccessButton size="lg"` → Task 1 Step 1. ✓
- Layout retune: drop headline `max-w`, keep readable subhead measure, tighten header→subhead gap to `mt-8`, backdrop unchanged → Task 1 Step 1. ✓
- Theme coherence in light/dark → follows from reusing existing theme tokens/classes; spot-checked in Step 4. ✓

**Placeholder scan:** No TBD/TODO/"similar to"/vague steps. All code shown in full. ✓

**Type consistency:** `Nav`, `AccessButton` import paths match `src/app/nav.tsx` / `src/app/access-modal.tsx`. `HeroBackdrop`/`Hero` defined and used within the one file. No undefined references. ✓

Note: this repo has no unit-test framework, so TDD's red/green steps are replaced by build + lint + visual verification — the available automated gates for a static marketing route.
